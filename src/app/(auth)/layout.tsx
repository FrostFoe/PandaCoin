// This layout is obsolete and no longer used.
// Kinde handles the authentication layout.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
